--- a/finalforeach/cosmicreach/TickRunner.java
+++ b/finalforeach/cosmicreach/TickRunner.java
@@ -62,7 +_,7 @@
 
       if (GameSingletonPlayers.getNumberOfPlayers() == 0 && !GameSingletons.isClient) {
          synchronized (this.thread) {
-            if (GameSingletonPlayers.getNumberOfPlayers() == 0) {
+            if (GameSingletonPlayers.getNumberOfPlayers() == 0 && finalforeach.cosmicreach.networking.server.ServerSingletons.running.get()) {
                this.pauseThread();
             }
          }
